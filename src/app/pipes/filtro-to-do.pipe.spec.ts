import { FiltroToDoPipe } from './filtro-to-do.pipe';

describe('FiltroToDoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroToDoPipe();
    expect(pipe).toBeTruthy();
  });
});
